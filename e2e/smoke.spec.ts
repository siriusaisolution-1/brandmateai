// e2e/smoke.spec.ts
// Placeholder for Playwright smoke tests.

// test('Pricing page should load', async ({ page }) => {
//   await page.goto('/pricing');
//   await expect(page.locator('h1')).toContainText('Pricing');
// });

// test('Signup page should handle query params', async ({ page }) => {
//   await page.goto('/register?plan=pro');
//   await expect(page.locator('#selected-plan-field')).toHaveValue('pro');
// });
