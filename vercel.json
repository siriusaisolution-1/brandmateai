{
  "buildCommand": "pnpm run clean && pnpm build",
  "installCommand": "pnpm -r i --frozen-lockfile",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "functions": {
    "maxDuration": 10
  }
}
